# Core ML
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24

# Lightweight production candidate (B1.3 baseline). Phase 3.3:
# prebuilt cp313/arm64 wheel может отсутствовать → собирается из исходников
# (требует Xcode CLT + pybind11; pybind11 идёт зависимостью).
fasttext-wheel>=0.9.2

# Sentence Transformers & SetFit
sentence-transformers>=2.2.0
setfit>=1.0.0

# Data Augmentation
nlpaug>=1.1.11
nltk>=3.8

# Optimization
optuna>=3.5.0

# Visualization
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# LLM Provider
together>=1.0
python-dotenv>=1.0

# JSON Schema
jsonschema>=4.20

# Diagrams
graphviz>=0.20

# Jupyter
ipywidgets>=8.0
jupyterlab>=4.0
nbformat>=5.0  # для AST-guard теста d1/tests/test_notebook_executor_only.py

# Utils
joblib>=1.3
pyyaml>=6.0
tqdm>=4.65

setfit
