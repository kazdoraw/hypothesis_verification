# D1: эксперимент по выбору лёгкого классификатора intent для AI-ядра
# Python 3.13+, macOS arm64 / Linux x86_64
#
# Установка из корня study/:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r d1/requirements.txt
#
# Этот файл — полный список зависимостей именно для эксперимента D1.
# Не дублирует общий study/requirements.txt: здесь только то, что
# реально импортируется кодом d1/.

# === Core ML ===
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3

# === B2.x baselines: sentence-transformers + torch ===
# B2.1 / B2.2 / B2.3 = BAAI/bge-m3 (568M params)
# B2.4 / B2.5       = intfloat/multilingual-e5-small (118M params)
sentence-transformers>=2.2
torch>=2.0

# === B1.3 baseline: fastText (sparse, ~1MB модель) ===
# Prebuilt cp313/arm64 wheel может отсутствовать → собирается из исходников
# (требует Xcode CLT на macOS).
fasttext-wheel>=0.9.2

# === Data loading / config ===
pyyaml>=6.0
python-dotenv>=1.0

# === Visualization ===
matplotlib>=3.7
seaborn>=0.12

# === Phase 5 data augmentation (Grok via OpenRouter) ===
# Нужен только для пересборки расширенного датасета через
# d1/scripts/generate_phase5_dataset.py. CSV-артефакты уже сохранены
# в d1/data/, поэтому стандартный прогон ноутбука httpx не задействует.
httpx>=0.27

# === Notebook environment ===
ipython>=8.0
ipywidgets>=8.0       # show_manual_query_sandbox() — интерактивный inference
jupyterlab>=4.0       # запуск ноутбука D1_domain_router_v6.ipynb
nbformat>=5.0         # d1/tests/test_notebook_executor_only.py (AST-guard)

# === Utilities ===
tqdm>=4.65

# === Testing ===
pytest>=7.0
pytest-cov>=4.0
